import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#343333',
        },
        secondary: {
            main: '#001575',
        },
    },
});

export default theme